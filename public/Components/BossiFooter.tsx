import React from 'react';
import { YouTube, Facebook, Instagram, Twitter } from '@mui/icons-material';
import { PageType } from './BossiHeader';

export interface BossiFooterProps {
  lightTheme: boolean;
}

export default function BossiFooter(props: BossiFooterProps) {
  return (
    <footer className='footer'>
    </footer>
  );
}
